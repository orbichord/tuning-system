# Installation

This is a simple library and as such its installation is mostly for direct development.

```bash
$ git clone 
$ cd tuning-systems
$ python -m venv .venv
$ pip install poetry
$ poetry install
```
